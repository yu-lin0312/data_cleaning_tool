# 開發慣例與約定 (NOTICE)

## Git 與版本發布約定
1. **自動提交與推送**：依使用者授權，每次功能修改或修復完成後，自動執行 `git add`、`git commit` 並 `git push origin main` 至 GitHub，無需每次額外詢問確認。
2. **版本號管理**：每次有顯著功能更新時，同步遞增 `index.html` 標題旁的版號標籤（如 `v1.3.0`），以利使用者辨識與排除快取。

## 核心設計原則
1. **輸入框穩定性**：左側原始資料輸入區維持原生 `<textarea>`，不使用 DOM 抽換或模式切換，杜絕破版風險。
2. **反白定位**：由表格或提醒盒跳轉原文時，直接使用原生的 `setSelectionRange` 與 `scrollTo` 進行高亮選取與捲動。
